@model Medlebox.Models.Song

@{
    ViewBag.Title = "Песня";
}

<h2>Плейлист</h2>

@using (Html.BeginForm("Delete", "Songs", FormMethod.Post, new { @class = "form-horizontal" }))
{

    <fieldset>
<h4>Удалить песню "@Html.DisplayFor(m=>m.Name)"?</h4>
<div class="form-actions">
                    <input type="submit" class="btn btn-large btn-danger" value="Удалить" id="btn-save" data-action="" />
    </div>
</fieldset>
}
